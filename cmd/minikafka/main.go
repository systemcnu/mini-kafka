// Command minikafka is the broker: it opens the data directory, listens on a
// loopback address by default, and stops gracefully on SIGINT/SIGTERM.
package main

func main() {
	// Filled in by build-order step 7 (broker) and step 8 (graceful stop).
}

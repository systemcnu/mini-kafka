# SL6 Slice Design — Protocol doc & repo completion

**Status: FINALIZED (2026-07-31) — after 2-seat independent review: 19 findings (seat A coverage/buildability 1 HIGH·4 MED·5 LOW; seat B implementation-reality, different-model, 1 HIGH·6 MED·2 LOW), 2 convergences, ALL integrated, none declined. Review record §7.**
**Derives from: SPEC v1.0.1 · DESIGN v1.0.1 · SLICES v1.0.1 (hashes in STATUS.md).** Scope authority: SLICES §2/SL6. Mechanisms referenced, never restated: DD-15/17 (wire registries), DD-26 (CI battery), DD-7 **as corrected at the SL5 gate** (fsync direction — accepted surfaced deviation, no erratum), SD-5 (partial proofs), SD-8 (registry convention), SD-9 (PROTOCOL.md budget), D-SL4-1 (AllCodes + source-parse audit), D-SL2-2 (GroupFetch paper trail).

Traceability root — the locked SLICES §1 row, verbatim:

> | SL6 | Protocol doc & repo completion | PROTOCOL.md a stranger could implement from + CI registry diff; full README pass; final PROT/OPS/NFR audits green on the public repo | K | 2.5 |

and SLICES §2/SL6 Builds (verbatim): "`docs/PROTOCOL.md` — budgeted at a full day; it must let a stranger implement a client (PROT-1) · CI registry-diff test · TOP-2's doc-audit half (no resize operation documented) · receipts record resolved runner versions (DD-26 completion) · full README pass (R1/R2/R3 statements, macOS fsync caveat per DD-7, port warning) · package doc-header audit (NFR-3) · final PROT-2/OPS-1/OPS-2/OPS-3/NFR-1 audits." Checks due: PROT-1, PROT-2, OPS-1, OPS-2, OPS-3, NFR-1, NFR-3, TOP-2's deferred half.

**Done =** scenario K real: a push to the public repo runs the whole battery green, including a registry-diff test that makes PROTOCOL.md↔`internal/wire` drift a build failure in both directions; the README is truthful against the as-built system (corrected fsync wording, R1/R2/R3 stated, bench markers untouched); TOP-2's doc-audit half — the LAST open partial proof — is closed and STATUS says so.

## 1. Spec contract table

| ID | Treatment | Notes |
|---|---|---|
| PROT-1 | Real | `docs/PROTOCOL.md` (D-SL6-1) + the two-leg registry-diff test (D-SL6-3) run in CI (D-SL6-4). Both halves of PROT-1's check text — message-type list AND error-code list — are machine-diffed. |
| TOP-2 (doc-audit half) | Real — closes the last SD-5 open half | The documented message set provably contains no resize operation: set-equality with the shipped registry (D-SL6-3) + an explicit no-resize subtest + the doc's fixed-partition statement (D-SL6-5). |
| PROT-2 (final audit) | Audit by command | Demo/bench/mk import only `client`; CLI smoke lives in CI's build-smoke job. Commands in D-SL6-8. |
| OPS-1 (final audit) | Audit + one CI fix | **Pre-identified gap: the binary build+smoke job runs on macOS only** — no Linux leg builds and smoke-runs the binaries (ci.yml `build-smoke: runs-on: macos-latest`). Fix = matrix, ledger row 6. |
| OPS-2 (final audit) | Audit by command | Green at HEAD; pins audit (Go, staticcheck, actions, digest-pinned demo container); mutable-runner honesty = echo-into-receipt per DD-26 — TWO label-runner jobs lack the echo line today, stdlib-audit AND vet-staticcheck (seat A's re-audit; D-SL6-4 completes both; demo-timing is exempt: digest-pinned container, its image identity is the digest itself plus its existing `cat /etc/os-release`). |
| OPS-3 (final audit) | Audit by command | README top screen carries the one demo command (true today, kept); LICENSE exists. |
| NFR-1 (final audit) | Audit by command | stdlib-audit job green at HEAD; command re-run at exit. |
| NFR-3 | Real (audit) | Package doc-header audit by command (all packages spot-checked clean during drafting — the audit makes it a recorded fact); vet+staticcheck green. |
| R1/R2/R3 README statements | Real | R1 stated today (kept) · **R2 (at-least-once/duplicates) is NOT stated in README today — must be added** (`grep -c 'at-least-once' README.md` → 0, whole file) · R3 stated, plus the port warning per SLICES' "port warning" — README today has NO `--addr` mention at all (both seats, convergence), so the warning lands on the existing loopback sentence, naming the flag (D-SL6-6c). |
| Scenario K | **Owned here** | Exit demo: a real push, all CI jobs green on the public repo, receipt captured. |

## 2. Design rows (D-SL6-n — each carries the check that proves it)

| # | Design row | Check that proves it |
|---|---|---|
| D-SL6-1 | **`docs/PROTOCOL.md` — content contract (SD-9's full day).** Hand-written, stranger-first, documenting the AS-BUILT protocol from these sources of truth: `internal/wire/frame.go` (envelope `[u32 len][u8 ver=1][u8 type][payload]`, big-endian, len covers everything after itself; asymmetric caps `MaxRequestFrame`/`MaxResponseFrame`; encoding primitives u8/u16/u32/u64 · str=[u16 n]bytes · blob=[u32 n]bytes; strict decode — truncation, hostile length, or trailing bytes = MALFORMED), `internal/wire/messages.go` (every message type 1–17 + 255 with a per-field table: field, wire form, meaning; empty bodies for types 6/7/14/16 stated; the request→response PAIRING made explicit in the diffed table — GroupFetch(17) is answered by FetchResp(4), no "GroupFetchResp" exists — plus the normative read-loop rule: every request is answered by its paired response type OR an Error frame (255), so a client reads the type byte FIRST and branches on 255 before matching the expected type), `internal/wire/errors.go` (all 13 codes — count derived by `grep -cE '^\tCode[A-Za-z]+ +Code = [0-9]+$' internal/wire/errors.go`), `internal/wire/names.go` (the name rule regex), `internal/broker/server.go` + `handlers.go` (connection lifecycle: one request in flight per conn; idle reclaim after 5 min SILENT close; error-then-close class — oversized frame, bad version, unknown type — vs error-then-continue class — malformed body, every semantic rejection; conn-cap CAP_EXCEEDED frame then close — UNSOLICITED: written immediately on accept before any request arrives, under a ~1 s write deadline, on a conn never entered in the broker's table, so a slow reader can miss the frame and see only EOF (stated); SHUTTING_DOWN drain: draining brokers answer every request with code 10, parked fetches return the empty shape; fetch validation order, defaults maxWait 0→5 s / maxBytes 0→1 MiB, the min-one budget rule WITH its accounting formula — each record costs 12 + len(payload) budget bytes (8-byte offset + 4-byte length prefix) and min-one is per-RESPONSE: only the FIRST record served across all entries in request order may exceed maxBytes (two brokers accounting differently would diverge — the formula is the interop contract) — the pinned empty-at-timeout shape: one zero-rec group per requested entry in request order, and the NORMATIVE read-deadline rule: a client's read timeout MUST exceed its requested maxWait, because the broker legally parks the response up to maxWait (≤30 s) and arms its write deadline only after dispatch — it will never write early), `internal/group/` (membership: control conn = liveness; heartbeat every 500 ms vs the 2 s session window, 100 ms sweep; immediate rebalance, range assignment, generations; REJOIN as a LEVEL in heartbeat flags bit0 — client must re-JoinGroup; join-carries-state; serve-time fencing with pinned precedence UNKNOWN_MEMBER-before-STALE_GENERATION; the 12-vs-13 SPLIT a client must implement differently — UNKNOWN_MEMBER(13) = identity gone, a fresh join mints a NEW member (generation bump, group-wide rebalance), STALE_GENERATION(12) = live member, re-Join on the same control conn ADOPTS the current generation with no bump, then reissue; 12/13 on a FETCH means re-join+reissue, never fatal; the parked-GroupFetch disposition during a rebalance is race-dependent and documented as such — a data-wake re-fences and returns 12, a timeout/shutdown returns the empty shape WITHOUT re-fencing; heartbeats exempt from the generation fence; commit = merge-onto-current then atomicWrite BEFORE the ack; commits naming partitions outside the member's assignment → STALE_GENERATION), durability semantics (ack = append→fsync→frontier→ack; read visibility capped at the durable frontier; the 5 ms group-commit window; committed offset = next-to-read; at-least-once, duplicates possible; **fsync platform-qualified in the CORRECTED wording** — `cmd/bench/report.go` lines 17–35 is the wording authority), a limits table (every DD-16 cap with its rejection code, citing the Go constant names), and a "what this protocol does NOT have" section (no partition resize — TOP-2; no topic delete; no auth/TLS; no Kafka compatibility; version 1 is the only version, types/codes are add-only, never renumbered). | The D-SL6-3 test green (tables ≡ registries) · section-presence grep (each named section exists) · a cold-read of the finalize review explicitly answers "could you implement a client from this alone?" (G-SL6-1 owns the residual). |
| D-SL6-2 | **The parsing contract — machine-checkable by construction (grammar pinned by BOTH seats, convergence).** The two registry tables sit between HTML-comment markers: `<!-- registry:codes:begin -->`/`<!-- registry:codes:end -->` and `<!-- registry:types:begin -->`/`<!-- registry:types:end -->`. Fixed grammar, stated in an HTML comment just INSIDE each begin marker so a future editor sees the contract in situ: between the markers the ONLY legal line classes are — blank lines · the in-situ grammar HTML comment · exactly one header row · exactly one separator row · data rows whose first cell parses as a base-10 integer. ANY other line is a hard test failure (strict-parse-or-fail — a lenient skip is silent drift). Cells split on UNESCAPED `|` only; a literal pipe in prose is `\|`. Codes table columns = `code | name | meaning | when sent`; types table columns = `type | name | direction | response | body summary` (the `response` column names the answering type — `FetchResp` for GroupFetch — or `—` for response/error rows; its presence makes the pairing diffable prose can't lose). Duplicate first cells rejected: parsed row count must EQUAL registry cardinality. Doc names are derived MECHANICALLY from the Go identifiers (ledger row 3): `CodeMsgTooLarge` → `MSG_TOO_LARGE` (strip `Code`, camel→upper-snake), `TypeGroupFetch` → `GroupFetch` (strip `Type`). | The D-SL6-3 test parses both tables under exactly this grammar; a format-breaking edit (moved marker, stray prose inside, renamed row, duplicated row) is a test failure, not silent drift — sabotage rows 5–6 in §5 prove it. |
| D-SL6-3 | **The registry-diff test (PROT-1's CI audit) — `internal/wire/protocoldoc_test.go`, test-only, zero production code.** Reads `../../docs/PROTOCOL.md` (package-dir-relative, same pattern as errors_test.go reading its own source). Leg A (codes): parse `errors.go`'s const block with `go/parser` (D-SL4-1's existing precedent) → (identifier, value) pairs → derived doc names; assert three-way equality: parsed set ≡ `wire.AllCodes()` ≡ the doc's codes table (names AND numeric values, both directions — a new code without a doc row fails; a doc row without a code fails; a renumber fails). Leg B (types): source-parse over ALL `.go` files in `internal/wire` (package-wide, hardened per seat B — not just `frame.go`), filtering const declarations by the `Type` NAME prefix AND underlying `byte` type (never by type alone — a future non-message `byte` const must not sweep in), asserting the parsed set ≡ the doc's types table both directions AND contiguity 1..17 + 255 (18 entries today, derived by `grep -cE '^\tType[A-Za-z]+ +byte = [0-9]+$' internal/wire/frame.go`) — a type added in ANY wire file without a doc row fails. Leg C: EVERY prose cell across both tables non-empty — `meaning`, `when sent`, `direction`, `response`, `body summary` — a row that names a code but explains nothing is a failure. Leg D (TOP-2): no documented type name matches `(?i)resize|alter|grow|shrink|repartition`, and the doc's fixed-partition statement line exists. | `go test ./internal/wire -run TestProtocolDoc -count=1` · red-before-green: the test lands BEFORE the tables are complete and is seen red · sabotage legs in §5. |
| D-SL6-4 | **CI placement + DD-26 completion.** ci.yml (5 jobs today, derived by `grep -cE '^  [a-z-]+:$' .github/workflows/ci.yml`) gains a sixth named job `protocol-doc` running the focused diff (`go test ./internal/wire -run TestProtocolDoc -count=1`) with the standard runner-echo line — DD-26's "PROTOCOL.md registry diff" job enumerated literally, and a visibly named check for repo readers. The test ALSO rides the existing test job's `go test ./...` (harmless double-run; `scripts/checks.sh` therefore covers it locally with zero script changes). DD-26 completion: BOTH label-runner jobs lacking it today — stdlib-audit AND vet-staticcheck (seat A's re-audit; the draft's "only job missing" was false) — gain the `echo "runner image= $ImageOS $ImageVersion"` line; demo-timing exempt, digest-pinned container. | Workflow grep (job present, echo lines in ALL label-runner jobs) · push → six jobs / SEVEN check runs (build-smoke × 2 after ledger row 6) green at HEAD, receipt. |
| D-SL6-5 | **TOP-2 doc-audit half — the last open partial proof, closed as a STANDING property.** The audit is not a one-time read: leg B proves documented-set ≡ implemented-set forever, leg D proves the set never contains a resize op, and PROTOCOL.md states "partition count is fixed at creation; this message set contains no resize operation" beside CreateTopic. STATUS's open-partials line updated to show all halves closed. | Leg B + leg D green in CI · `grep -n 'no resize' docs/PROTOCOL.md` · STATUS diff at exit. |
| D-SL6-6 | **Full README pass — truth against the as-built system, outside the bench markers ONLY.** (a) **Fix the self-contradiction found while drafting:** README line 56's prose carries DD-7's OLD inverted claim ("fsync on macOS may not flush the drive cache") while the machine-rendered bench block inside the markers already carries the corrected one — replace the prose caveat with the corrected wording, reusing the bench caveat sentence verbatim (ledger row 7). (b) Add the R2 statement: delivery is at-least-once; duplicates are possible after crashes/rebalances, loss is not. (c) Keep R1 (index growth, no retention — present today) and R3; the port warning lands on the EXISTING loopback sentence — README has NO `--addr` mention today (both seats, convergence; the flag's own usage string already warns) — extending that sentence to name the flag with the pinned phrase "an unauthenticated protocol": the broker binds 127.0.0.1 by default, and pointing `--addr` anywhere else exposes an unauthenticated protocol to that network. (d) Link `docs/PROTOCOL.md` from the README ("implement your own client from…"). (e) Top screen untouched (OPS-3 true today). (f) **The bench-marker section is NEVER hand-edited** — the armed BENCH-3 byte-compare proves it stayed intact. | `grep -c 'may not flush' README.md` → 0 · `grep -c 'at-least-once' README.md` ≥ 1 (whole file — the armed render gate proves it cannot be satisfied from inside the markers) · `grep -c 'unauthenticated protocol' README.md` ≥ 1 (the port warning, pinned — seat A: it had NO check) · render gate (existing `render_test.go` real-README leg) still green · README top-screen audit. |
| D-SL6-7 | **D-SL2-2's paper trail discharged in PROTOCOL.md.** The doc records both logged facts where a stranger needs them: (1) at GroupFetch — why it exists (group fetches must carry memberID+generation for serve-time fencing; Fetch has no such fields; topic is implied by the group's binding); (2) at Fetch — the multi-entry form (2..16 entries) is a fully supported protocol shape with the honest note that the shipped client happens not to use it raw (GroupFetch carries the same multi-entry tail). This also documents, without re-flagging, that GroupFetch superseded DD-15's prose picture — surfaced and accepted at the SL2 gate. | Pinned phrases, one per note (seat A: bare presence-grep proved nothing): `grep -c 'serve-time fencing' docs/PROTOCOL.md` ≥ 1 at the GroupFetch note · `grep -c 'no shipped-client caller' docs/PROTOCOL.md` ≥ 1 at the Fetch multi-entry note · leg B covers the type's presence mechanically. |
| D-SL6-8 | **Final audits, each a command.** PROT-2: `go list -deps ./cmd/demo ./cmd/bench ./cmd/mk | grep mini-kafka` shows `client` (+ `internal/*` only via broker-hosting in demo/bench, the D-SL5-1/D-SL3-1 precedent) — no wire-level privileged path in any CLI tool; CLI smoke = CI build-smoke job. OPS-1: matrix build+smoke green on BOTH platforms after ledger row 6's fix. OPS-2: `gh run list -L1` green at HEAD; pins grep (go.mod `go 1.24`, setup-go `1.24.x`, staticcheck `@2025.1.1`, demo container digest, actions `@vN`). OPS-3: top-screen + `test -f LICENSE`. NFR-1: stdlib-audit job green. NFR-3: doc-header audit — `go list ./... | while read -r p; do go doc "$p" | grep -q '^Package ' || { echo "MISSING doc header: $p"; exit 1; }; done && echo ALL-DOCUMENTED` (seat A killed the draft's `sed -n 2p`, which emits a blank line for documented and undocumented packages alike; this one fails loudly, naming the package) + vet/staticcheck green. Each command's output lands in the exit receipt. | The commands above, outputs receipted in `docs/receipts/sl6-audits.txt`. |
| D-SL6-9 | **Diagrams — published .md, so baked by rule; count/selection is ledger row 11's recorded choice.** PROTOCOL.md gets exactly two: (1) the frame/record byte layout as fenced ASCII (monospace text renders in EVERY viewer — no bake needed, and byte layouts read better as ASCII than mermaid); (2) ONE mermaid sequenceDiagram of the group lifecycle (join → assignment → heartbeat/REJOIN → re-join → fenced late commit), baked via `python3 ~/.claude/templates/bake_mermaid.py docs/PROTOCOL.md` → PNG primary in `docs/diagrams/`, source in `<details>`. Mermaid discipline: no `;` in message/note text (repo-wide rule). README gets no new diagrams. | `grep -rn '^```mermaid' docs/PROTOCOL.md README.md` → 0 raw fences · PNG exists and is >0 bytes · `mmdc` render check before bake. |
| D-SL6-10 | **Touch list + honest change accounting (SL4's lesson).** NEW: `docs/PROTOCOL.md`, `docs/diagrams/*`, `internal/wire/protocoldoc_test.go`, `docs/receipts/sl6-*.txt`. EDITED: `README.md` (outside markers), `.github/workflows/ci.yml` (protocol-doc job · stdlib echo · build-smoke matrix), `STATUS.md`, code map regen. **ZERO production-code changes** — no broker/client/wire/storage/group `.go` file changes; if the build surfaces one, it stops and gets its own ledger row first. CLI polish (G-SL2-1's offer) declined — ledger row 9. Redaction rule: the scrubbed registration address appears in NO file — including THIS one and the receipts (the grep pattern itself must never be committed; the check names the count, not the pattern). | `git diff --name-only` ⊆ the list above · redaction grep (pattern held outside the repo) → 0 matches repo-wide at exit, count receipted. |

## 3. Decision ledger (choices not dictated by locked artifacts)

| # | Decision | Why | Consequence if wrong |
|---|---|---|---|
| 1 | PROTOCOL.md stays HAND-WRITTEN with a diff test, not generated from code. | SD-9 calls it real writing for a stranger; generation guarantees sync but produces reference-dump prose that fails the "could implement from" bar. The diff test buys the sync guarantee for exactly the two lists PROT-1 names, keeping the prose human. | Prose can still drift from behavior (G-SL6-2); the live battery + review are the backstop. |
| 2 | **Message types DO get the same diff treatment — via source-parse, not a new `wire.AllTypes()`.** The "whether" is not a choice: PROT-1's check text literally requires the message-type list to match the registry. The "how" (hardened by seat B): parse ALL `.go` files in `internal/wire` — not one file — filtering by the `Type` name prefix + `byte` type, and assert contiguity 1..17 + 255, instead of adding a production `AllTypes()` function. | Keeps the slice doc+test-only; an AllTypes() would be production surface with no runtime caller; package-wide parse + contiguity closes seat B's false-green (a type added in a different wire file). | Residual, accepted: a type constant declared OUTSIDE `internal/wire` is invisible to the parse — and also unreachable by the envelope decoder, which lives in wire. |
| 3 | Doc names derived mechanically from Go identifiers (camel→upper-snake for codes, strip-prefix for types); no hand-maintained name map anywhere. | A hand map in the test is a second registry that can itself drift; derivation makes the identifier the single name authority. | Derived names are frozen into public doc text; renaming a Go constant later changes the doc's expected name — caught red by the diff, resolved deliberately. |
| 4 | A NAMED `protocol-doc` CI job (sixth job), even though the test also rides the test job. | DD-26 enumerates "PROTOCOL.md registry diff" among the Jobs — the named job satisfies the locked list literally, and a visible check is itself portfolio signal (this repo's job list is part of scenario K's story). | One redundant ~seconds job per push; zero drift risk since both run the same test. |
| 5 | The limits table is NOT machine-diffed (types + codes only). | PROT-1's check names exactly two lists. The caps live as constants across four packages (`wire`, `broker`, `group`, `storage`); diffing them means new exported surface or a fragile four-file parse — gold-plating. The table cites each Go constant by name, so the human audit is one grep per row; SL4's per-cap rejection tests already pin the values behaviorally. | A silently changed cap could outdate the doc's number until a human notices; the per-cap tests make the value itself regression-proof, only the doc copy can lag. |
| 6 | **The one CI-behavior change beyond the new job: `build-smoke` becomes a two-OS matrix (ubuntu + macos).** | OPS-1's check is literal — "CI matrix with a native smoke run on both" — and today only macOS builds and smoke-runs the binaries (Linux runs tests and the containerized demo, but never the built `minikafka`/`mk` smoke). Audit-driven fix, surfaced at the gate, not silently absorbed. | If the Linux smoke exposes a platform bug, that is the audit doing its job — fixing it would then be its own accounted change. |
| 7 | README's corrected fsync prose reuses the bench caveat sentence VERBATIM (`report.go` lines 30–35's text). | One wording, one authority — two paraphrases of a subtle platform fact is how the SL5-corrected claim regresses. PROTOCOL.md's durability section cites the same sentence. | None foreseeable; verbatim reuse is greppable. |
| 8 | PROTOCOL.md separates NORMATIVE client obligations from informative notes (the shipped client's redial behavior). The normative list, expanded by seat B: heartbeat ≤ 500 ms cadence against the 2 s session window · re-JoinGroup on REJOIN · the 12-vs-13 SPLIT, implemented differently (13 → fresh join, new identity, group-wide rebalance; 12 → re-Join adopts the current generation, no bump, then reissue; either on a FETCH = re-join+reissue, never fatal) · the read-deadline rule (read timeout MUST exceed requested maxWait — seat B's HIGH: without it a fixed-timeout long-poll client kills its conn on every empty poll). | A stranger implementing a client needs to know which behaviors the broker ENFORCES (sweep, fence) vs which are conventions; blurring them is how "works with my client" bugs ship. | Marginal doc length. |
| 9 | G-SL2-1's "SL6 owns any final CLI polish" is DECLINED: no CLI code changes. | No SPEC ID demands CLI features beyond what shipped (PROT-2 is satisfied); this slice is doc + CI-test only by scope, and the README already documents `mk` accurately. | If Sri wants polish it is a named post-v1 item, not a silent scope creep. |
| 10 | The diff test lives in package `wire` (`internal/wire/protocoldoc_test.go`), reading the doc via package-relative path. | The test needs `go/parser` over wire's own sources (precedent in errors_test.go) and `AllCodes()`; `go test` runs with the package dir as cwd, so `../../docs/PROTOCOL.md` is stable. A separate audit package would re-implement the parse for no gain. | If docs/ ever moves, one constant changes — loudly. |
| 11 | PROTOCOL.md gets exactly TWO diagrams — the frame/record byte layout as fenced ASCII (not mermaid: monospace renders in every viewer and byte layouts read better as text) and ONE baked mermaid sequence for the group lifecycle; README gets no new diagrams. | Seat A: the bake RULE is repo-wide but the count/selection was a real undictated choice hiding in D-SL6-9's prose. Two is the minimum covering the doc's two hard-to-prose shapes (envelope bytes, membership dance). | If a reader needs a third shape drawn, that is an issue-driven addition, not a defect. |

## 4. Known gaps accepted

- **G-SL6-1** — "a stranger could implement from it" is not mechanically provable. The mechanical floor is tables ≡ registries + non-empty semantics cells; the human proxy is the finalize review's cold read answering that question explicitly. Residual risk: an ambiguity only a real stranger would hit — accepted; the doc invites issues.
- **G-SL6-2** — the diff proves LIST completeness, not prose truth: a wrong "when sent" sentence passes the test. Mitigation: every behavioral claim in the doc is written against a named source file/test (D-SL6-1's source-of-truth list), and SL4's live battery already proves each code is elicitable as described.
- **G-SL6-3** — README truth outside the bench markers is human-maintained; only the bench section is byte-gated. Accepted repo-wide (same posture since SL0); this slice's pass resets it to true.
- **G-SL6-4** — normative client obligations (ledger row 8) are enforced by liveness/fencing, not by protocol errors naming the violation — a client that heartbeats too slowly is swept, not lectured. Stated in the doc.
- **G-SL6-5** — the limits table's numeric copies can lag a changed constant (ledger row 5's accepted residue).

## 5. Test plan mapped to claims

- **Registry-diff test** (`internal/wire/protocoldoc_test.go` — legs A–D per D-SL6-3): written FIRST against the not-yet-tabled doc and seen RED (missing markers → named failure, not a pass-by-absence: absent markers/file is a hard fail from day one), then green as the tables land. Sabotage matrix at exit (integrator's hands, any one suffices for the gate; all SIX run locally): delete a codes row → red · add a fictitious `ERR_RESIZE_TOPIC` row → red (legs A and D both) · renumber a types row → red · blank a meaning cell → red · insert a stray prose line between the markers → red (strict-parse — seat A's fifth row) · duplicate an existing doc row → red (count-equality — seat A's F8).
- **README checks:** `grep` battery per D-SL6-6 (old inverted phrase count 0 · R2 statement present · corrected sentence present outside markers) · the EXISTING armed render gate (`cmd/bench/render_test.go` real-README leg) proves the bench section byte-identical through the whole pass.
- **CI:** push → six jobs green at HEAD, including `protocol-doc`; receipt records the run URL + resolved images (DD-26 lines now in every label-runner job).
- **Audit receipts:** every D-SL6-8 command's output captured to `docs/receipts/sl6-audits.txt`.
- **Bake check:** zero raw mermaid fences in published .md (`grep -rn '^```mermaid' docs/ README.md`) · baked PNG present, >0 bytes.
- **Redaction check:** the scrubbed-address grep (pattern held OUTSIDE the repo — committing the pattern would itself violate the scrub) → 0 matches repo-wide; the receipt records the count, never the pattern.

## 6. Validate — exit checklist (all demonstrated, not asserted)

1. Red-before-green: the diff test seen red (no/incomplete tables) then green; receipt.
2. Full suite + `scripts/checks.sh` race-green by command (count reported by command, expected to grow only by the new wire test).
3. Scenario K: push to the public repo → six jobs / seven check runs (build-smoke × 2) green at HEAD; receipt with run URL.
4. Exit sabotage (integrator's hands): one sabotage-matrix row executed against CI-equivalent local run → red → restored → green.
5. TOP-2 closure recorded: STATUS open-partials line shows every SD-5 half closed.
6. Audits receipted (`sl6-audits.txt`); OPS-1 matrix fix landed and green on both platforms.
7. Code map regen + BRIEF (baked diagram) + STATUS/LAB-STATE + commits.
8. Gate notes for Sri: (a) DD-7-corrected wording now in README prose + PROTOCOL.md — executing the SL5-gate acceptance, not a new deviation; (b) **surfaced-deviation candidate: OPS-1's Linux binary-smoke gap + the matrix fix (ledger row 6)**; (c) PROTOCOL.md documents 13 codes where DD-17's locked prose lists 12 — already surfaced at SL4 (D-SL4-6), referenced; (d) GroupFetch vs DD-15's prose — already surfaced at SL2 (D-SL2-2), referenced; (e) CLI polish declined (ledger row 9).

## 7. Review record (FINALIZE, 2026-07-31)

2 seats, fresh contexts, neither reviewed its own writing; reviewers proposed, the integrator applied. **Seat A (coverage & buildability): 10 findings** — HIGH: the DD-26 echo audit claimed stdlib-audit was "the only job missing" the runner-echo line while vet-staticcheck also lacks it (a builder following the row would ship DD-26 incomplete believing it done) · MED: port warning had no check anywhere / README has no `--addr` mention (false premise) / NFR-3's audit command provably vacuous / strict-grammar property claimed but never sabotage-tested · LOW: job-count wording after the matrix, R2 grep scoping, duplicate-row false green, D-SL2-2 presence-grep, diagram choice missing from the ledger. **Seat B (implementation-reality, different-model): 9 findings** — HIGH: no normative read-deadline-exceeds-maxWait rule (a stranger's fixed-timeout long-poll client kills its conn on every empty poll) · MED: types leg false-greens a type added outside frame.go / request→response pairing undiffed (GroupFetch→FetchResp, 255-first read loop) / parked-fetch disposition on rebalance race-undocumented / 12-vs-13 obligations flattened / grammar escaped-pipe+filter holes / min-one budget formula missing (interop divergence) · LOW: CAP_EXCEEDED unsolicited+1 s window, `--addr` retarget (= seat A's F3). **Convergences (2):** the nonexistent `--addr` mention (A-F3 ≡ B-F9) · the table grammar needing pinning from both directions (A-F5 + B-F6, merged into D-SL6-2). **All 19 integrated above; none declined.**

---
Sizing against the 2.5d budget: PROTOCOL.md 1.0d (SD-9's floor) · diff test + CI wiring 0.5d · README pass + audits + matrix fix 0.5d · exit process 0.5d (SD-6).

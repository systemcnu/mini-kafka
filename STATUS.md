# STATUS — mini-kafka (public name, resolved at SPEC lock)

**Updated: 2026-08-01 (SL7 passed — the locked plan is COMPLETE: SL0–SL7 all passed, every scenario A–K demonstrated and gated; mini-kafka v1 done).** Project authority for this repo. Lab/process view: `~/skills/process/LAB-STATE.md`.

## Where things stand

- **SPEC: LOCKED v1.0.1** (locked 2026-07-24; v1.0.1 redaction erratum 2026-07-28, accepted at the SL0 gate). 37 active IDs. Hash: `50237dd479caf8bbf7268d92304699efaaf0ed9834646d483bf3d018e313dcfe`. Read `SPEC-BRIEF.md`.
- Locked by Sri after the 3-question quiz (two re-asks, both then answered on the brief's exact lines) and four gate decisions, recorded in SPEC §7: name stays "mini-kafka" · MIT license · durable-only benchmarks · showcase kept conditional.
- Lie-hunt record: 5 seats (4 Claude — ambiguity 10, untestability 10, gaps 9, contradiction/goal-fit 8 — plus Codex feasibility 9) = 46 findings, all integrated or gate-resolved. Orphan-ID audit clean.
- **DESIGN: LOCKED v1.0.1** (locked 2026-07-28; erratum E1 accepted at the SL0 gate — crash-walk wording, no behavior change). 26 DD rows, coverage 37/37. Hash: `5d48d8ac7b795ca4306d0cd5eca3038ef3cded4fafb5f0ce789c4b99df6eae08`. Read `DESIGN-BRIEF.md`.
- Locked by Sri after the 3-question quiz (two clean, disk-error question answered correctly on its one re-ask). Lie-hunt record: 4 seats (coverage 8, buildability 9, simplicity 10, Codex platform-reality 10) = 37 findings — all integrated, one declined with written reason (DESIGN §10).
- **SLICES: LOCKED v1.0.1** (restamp + redaction, SL0 gate). 8 slices, 24.5d, A–K owned exactly once. Hash: `273187dc7b2c9b9cf5d9a22d1aba1a25922dacba988dc9806e2d3a9f40136cd3`. Read `SLICES-BRIEF.md`.
- Locked by Sri after the 2-question quiz (clean). Gate decisions: **B1 — publish at SL0 exit (public early) · B2 — Sri's existing GitHub account.** Lie-hunt record: 4 seats = 33 findings, all integrated (v0.1's skeleton had no broker; sizing under ~1.6x).
- **SL0 (walking skeleton): PASSED at Sri's gate 2026-07-28** (verdict: pass; erratum E1 accepted; email redaction accepted; module renamed to github.com/systemcnu/mini-kafka). 51 tests green (race-checked), red-before-green receipted (216-line receipt), ack-ordering sabotage witnessed red and restored, scenario B live transcript committed, full local CI battery green, code map 84/84 anchors. Read `slices/SL0/BRIEF.md`.
- **SL1 (crash & disk hardening): PASSED at Sri's gate 2026-07-29** (verdict: pass). Slice design FINALIZED after 2-seat independent review (9 findings, all integrated; worst: the draft's truncate-back could brick a partition on a post-rename frontier-write failure — shipped rule scoped per D-SL1-3). Built: storagetest fault fakes, full recovery matrix, degrade()+truncate-back, broker FS seam + wire code-11 proof, topic-create abort tests, kill -9 harness (3 SIGKILL cycles, 44 journaled acks re-verified after every kill). 80 tests green race-checked (up from 51), red-before-green receipted per file, exit sabotage witnessed red (defense-in-depth guard also caught it), scenario J live on a 10 MB APFS image (receipt). One CI failure on push (staticcheck ST1008) — fixed; checks.sh now runs the pinned staticcheck, local battery truly CI-equivalent. Read `slices/SL1/BRIEF.md`.
- **SL2 (consumer groups): PASSED at Sri's gate 2026-07-29** (verdict: pass). Slice design FINALIZED after 2-seat review (13 findings, all integrated; headline: heartbeat generation-fencing would have made REJOIN undeliverable + falsely swept live members · commit lost-update caught INDEPENDENTLY by both seats · rebalance livelock · park goroutine leak · the locked `mk consume --group` demo line restored). Built: wire types 9–17 + codes 12/13, `internal/group` coordinator (one mutex, injectable clock, level-triggered REJOIN, conn-bound re-Join), merge-inside-lock durable commits with re-fence-at-install, `_groups` boot reservation, TryFetch + multi-entry park (G7 closed), dual-conn GroupConsumer, `mk consume -g`. 128 tests green race-checked (up from 80), red-before-green receipted, exit sabotage (serve-time fence) witnessed red in 2 tests, SD-11 SIGSTOP fencing run LIVE on real mk processes — live demo caught a real mk defect all tests missed (fatal-exit on routine rebalance collision; fixed: continue on 12, exit on 13). Receipts: sl2-red-green.txt, sl2-kill9-commits.txt, sl2-scenario-e.txt. Read `slices/SL2/BRIEF.md`.
- **Open partial proofs (SD-5 tracking): NONE — all closed.** CONS-1 multi-entry service — CLOSED at SL2 · scenario C group-positions half — CLOSED at SL2 · TOP-2 doc-audit half — CLOSED at SL6 as a standing machine-checked property. (LOG halves closed at SL1; CI-green-on-GitHub closed 2026-07-28.)
- **SL3 (the demo): PASSED at Sri's gate 2026-07-29** (verdict: pass; DD-21 digest-wording deviation accepted as surfaced). Design FINALIZED after 2-seat review (13 findings integrated; headline: the narration needed a client Assignment() accessor that didn't exist · Abandon()'s naive lifecycle mutex-trapped the kill behind a 7 s park · gated-vs-tested surface never coincided [both seats] · public README still denied groups existed — corrected now). Built: cmd/demo (two acts, deterministic byte-identical transcript, #event markers), client Assignment()+Abandon(), scripts/demo_timing.sh external clock (GOTOOLCHAIN preflight, direct-pipe timestamper, 4 receipted red paths), CI demo-timing job in a digest-pinned golang:1.24 container, README top screen + truth pass. **MEASURED: first-flow 6 s / total 18 s in CI's cold container (gates 60/180) · macOS cold leg 2 s / 18 s (cold-cache isolation verified by a 1.85 s controlled probe).** 131 tests race-green (by command), scenario A live receipt, marker sabotage exit-5 red witnessed, CI receipt captured from the real run. DEMO-1/DEMO-2 checks fully discharged. Receipts: sl3-scenario-a.txt, demo-timing.txt, demo-timing-macos.txt, sl3-red-green.txt. Read `slices/SL3/BRIEF.md`.
- **SL4 (hostile inputs & caps audit): PASSED at Sri's gate 2026-07-30** (verdict: pass; the four gate notes — SD-10→SD-8 misnumber, 13-vs-12 code provenance, clock-seam-as-duration, the one client change — accepted as surfaced). Design FINALIZED after 2-seat review (14 findings, all integrated; two convergences; worst: the completeness audit as drafted audited its own list — a forgotten constant passed silently; the draft's "clients reconnect" claim was false for this repo's own client). Built: `wire.AllCodes()` + source-parser audit leg, the 13-code live battery (`TestScenarioH` = scenario H, three instances), conn-cap served CAP_EXCEEDED frame (wg-tracked writer), idle reclaim (Config.IdleTimeout, read deadline per frame-wait + response write deadlines), GroupConsumer Poll redial on EOF/reset, broker-level malformed table, group caps over the wire, fetch-side bad-partition. 143 tests race-green by command (up from 119), red-before-green receipted, exit sabotage (payload cap) witnessed red in battery AND per-cap test. Integrator catch: the builder's stop-hammer passed 30 isolated runs but missed 20/20 under full-suite load — de-flaked by scripting the drain window through the Syncer seam + a second collecting conn (receipted). Gate notes: SD-10→SD-8 misnumber in locked SLICES · 13-vs-12 code provenance · "clock seam" realized as duration seam · the one client change. Receipts: sl4-scenario-h.txt, sl4-red-green.txt. Read `slices/SL4/BRIEF.md`.
- **SL5 (benchmarks): PASSED at Sri's gate 2026-07-31** (verdict: pass; the DD-7 fsync-direction correction accepted as a surfaced deviation per the brief's say-nothing-=-accept convention — no erratum; SL6's doc pass carries the corrected wording into README/PROTOCOL.md). Design FINALIZED after 2-seat review (16 findings, all integrated; 3 convergences; worst: `go run` embeds no VCS info — the reference report's commit label would have been EMPTY at the exit demo; the README block carried 6 of BENCH-2's 14 labels). Built: `cmd/bench` closed-loop harness (8 producers, 1 GroupConsumer, dedupe-before-sampling, boundary commits, nearest-rank percentiles, spread, GC deltas), provenance-gated reports (`-hardware` required, VCS refusal, `-dirty` stamping), `-render-readme` + the armed BENCH-3 byte-compare gate. **Reference run committed (Apple M3 Max): ~40–100 msg/s, ack p50 70–140 ms, one 3.7 s p99 stall — honest.** Integrator findings: DD-7's macOS fsync caveat is INVERTED for Go (probed: Sync = F_FULLFSYNC 5.5 ms vs raw fsync 0.11 ms — stronger AND slower; labels corrected, gate call in BRIEF Q2) · dirty-stamp gate caught my own first build · smoke's under-load member-sweep abort bounded by a loud single retry (6 consecutive full-suite greens, one exercising it). 152 tests race-green by command (up from 143). Receipts: sl5-scenario-g.txt, sl5-red-green.txt. Read `slices/SL5/BRIEF.md`.
- **SL6 (protocol doc & repo completion): PASSED at Sri's gate 2026-07-31** (verdict: pass; all four brief choices accepted under say-nothing-=-accept — the build-smoke two-OS matrix, the NFR-3 audit-form correction, the two already-accepted-deviation references, CLI polish declined). Read `slices/SL6/BRIEF.md`. First slice run under the stage-loop context discipline (drafts/build/verification all delegated to fresh subagents; integrator applied findings and witnessed terminal gates bare). Design FINALIZED after 2-seat review (19 findings, 2 convergences, all integrated; worst: the missing read-deadline-exceeds-maxWait rule a stranger's long-poll client dies on · the DD-26 echo audit undercounted missing jobs). Built: `docs/PROTOCOL.md` (stranger-implementable; 13 codes + 18 types machine-diffed both directions by `internal/wire/protocoldoc_test.go` under a strict marker-table grammar), README truth pass (fsync self-contradiction fixed, R2 at-least-once added, port warning pinned), CI protocol-doc sixth job + two DD-26 echo lines + build-smoke two-OS matrix, D-SL2-2 paper trail discharged, TOP-2 doc-audit half CLOSED (all SD-5 partial proofs now done). NFR-3 audit-form erratum: draft vacuous → seat A's fix convention-wrong → builder honest STOP → integrator corrected to `go list .Doc` (receipted). Verifier (not the builder): 3 consecutive full-suite race runs green, 6/6 sabotage red-restore-green, 9 prose-vs-code claims clean, cold read YES(client)/YES-with-gaps(broker) — both gaps fixed same day. 147 test functions by command (146 before), zero production .go changed. Receipts: sl6-red-green.txt, sl6-audits.txt.
- **PUBLISHED: github.com/systemcnu/mini-kafka (public, CI green at HEAD — six jobs after SL6's push: protocol-doc joins, build-smoke × 2 OS).** Sri authorized on-their-behalf pushes for this repo (2026-07-28). **SL7 (showcase build): PASSED at Sri's gate 2026-08-01 — the BUILD ending fired; with it the locked plan is COMPLETE.** (Verdict: pass; all four brief choices accepted under say-nothing-=-accept — the DD-23 allowance-verification downgrade, the 8080 edge-posture correction, the mid-procedure scan-script fix, the teardown criterion as live policy. SHOW-2 disposition per the gate-disposition section of `docs/receipts/sl7-deploy.md`: no card-shown correction was sent with the verdict under the brief's mandatory-if-true framing; the literal attestation sentence was never typed — transcribed honestly, not quoted.) Read `slices/SL7/BRIEF.md`. Phase A (local, 16-finding review incl. the fatal-concurrency catch, 6 reds receipted, verifier 4/4 sabotage + 28 race warnings on the sync-removal leg) pushed CI-green BEFORE any account existed; Sri's checkpoint returned the hostname; Phase B live: port scan green after two live catches (edge drops packets → -G fix, self-test re-proven · 8080 = Render's shared-edge Blocked page, not this service — expected posture corrected, investigated in receipt), live route probes 405/404, scenario I receipted with a TRUE cold start (feed's own startedAt 19 s after open; swap-in ~13 s; produced 69→150 across 40 s; disk 1.8 KB vs the 200 MiB cap; heap observable). 163 test functions (147 before); zero production-code and zero CI changes (7 check runs); scrub battery 0 with its own false-positive correction receipted. Gate needs: verdict + the dated no-card attestation (Sri's checkpoint reply was the hostname only). **Next: nothing — the plan has no further slices.** Standing items only: the teardown criterion is live policy (Sri owns the trigger — card demand / charging / hours exhausted → delete the service, the link reverts to "not currently hosted"); the registry-diff, demo-timing, and bench render gates keep guarding every future push; any new work is new scope through a new spec. Showcase feasibility: PASS receipt on file.

## Grill record

8 questions, 2026-07-24: portfolio piece · success = 60-second demo + honest benchmarks · local + optional hosted · watch-only self-driving showcase · core + consumer groups tier · four explicit OUTs · Go · $0-hard free-tier-only hosting.
